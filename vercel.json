{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "frontend/web-build",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
